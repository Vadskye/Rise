---
description: Generate LaTeX content for classes, monsters, and equipment using the Rust project.
---

1. Ensure you are in the `rust` directory or the root.
// turbo
2. Run the generation script:
   ```powershell
   ./rust/src/bin/generate_rust_files.fish
   ```
   Note: On Windows, use a compatible shell or the equivalent command if needed.

> [!IMPORTANT]
> Always ignore the `rust/target` directory in searches.
