#!/usr/bin/env fish

set sphere "$argv[1]"

g rts
tsc --incremental
node src/scripts/spell_rank_breakdown.js --spheres="$sphere" "$argv[2..]"
echo ""
node src/scripts/spell_role_breakdown.js --spheres="$sphere" "$argv[2..]"
